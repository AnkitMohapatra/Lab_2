#include <iostream>
#include<cmath>
using namespace std;

int main() {
float a,b;
cout << "Enter both the angles respectively(in degrees) : ";
cin >> a >> b;
cout << "Third angle = " << 180-(a+b);
return 0;
}
